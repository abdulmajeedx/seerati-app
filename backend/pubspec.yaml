name: seerati_backend
description: Seerati AI backend — proxies Claude API with quotas and activation.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  http: ^1.2.2
  sqlite3: ^2.4.7
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
